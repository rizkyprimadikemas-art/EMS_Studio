import 'package:flutter/material.dart';

class PZEMPage extends StatelessWidget {
  const PZEMPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'PZEM',
        style: TextStyle(fontSize: 28),
      ),
    );
  }
}